services:
  beszel:
    container_name: beszel
    image: henrygd/beszel:0.18.7
    # ports:
    #   - "8090:8090"
    environment:
      - APP_URL=https://beszel.karnwong.me
    volumes:
      - /opt/beszel/data:/beszel_data
      - /opt/beszel/socket:/beszel_socket
    restart: always
  # ---- caddy ----
  caddy:
    image: caddy:2
    container_name: caddy
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile.oracle2:/etc/caddy/Caddyfile:ro
      - /opt/caddy/data:/data
