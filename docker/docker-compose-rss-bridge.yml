version: '3.3'

services:
  rss-bridge:
    container_name: rss-bridge
    image: rssbridge/rss-bridge:latest
    restart: always
    ports:
      - 81:80
    networks:
      - rss-bridge

networks:
  rss-bridge:
    driver: bridge
