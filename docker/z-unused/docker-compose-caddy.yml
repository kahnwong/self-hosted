---
version: '3.3'

services:
  caddy:
    ports:
    - 80:80
    - 443:443
    image: caddy/caddy
    volumes:
    - /opt/caddy/data:/data
    - ./caddy/config/Caddyfile:/etc/caddy/Caddyfile
    restart: always
    extra_hosts:
    - host.docker.internal:host-gateway
    network_mode: host
