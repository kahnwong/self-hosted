version: "3"

services:
  dashboard:
    image: b4bz/homer:latest
    container_name: homer
    restart: unless-stopped
    volumes:
      - ./homer/assets:/www/assets
#      - ./www:/www
    ports:
      - 9533:8080
    networks:
      - homer

networks:
  homer:
    driver: bridge
