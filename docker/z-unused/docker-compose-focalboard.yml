---
version: '3.3'

services:
  focalboard:
    container_name: focalboard
    image: mattermost/focalboard:7.0.1
    restart: always
    networks:
    - focalboard
        #    volumes:
        # - '/opt/focalboard/data:/data'
        #- '/opt/focalboard/config:/opt/focalboard'
    ports:
    - 89:8000
    environment:
      TZ: Asia/Bangkok

networks:
  focalboard:
    driver: bridge
