services:
  glance:
    container_name: glance
    image: glanceapp/glance
    restart: unless-stopped
    env_file:
      - .env.app.glance
    volumes:
      - ./config:/app/config
    ports:
      - 8080:8080
