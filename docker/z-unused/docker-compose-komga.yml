---
version: '3.3'

services:
  komga:
    image: gotson/komga:latest
    container_name: komga
    volumes:
      - /opt/komga/config:/config
      - /opt/syncthing/magazines:/books/Magazines:ro
      - /opt/syncthing/comics:/books/Comics:ro
    ports:
      - 4946:8080
    restart: always
    networks:
      - komga

networks:
  komga:
    driver: bridge
