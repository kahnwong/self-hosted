name: backup-miniflux
schedule: "2 0 * * *"
containers:
  - name: backup-miniflux
    repository: ghcr.io/kahnwong/docker-aws-backup
    tag: f09a8ec
    env:
      - name: MODE
        value: DB_POSTGRES
      - name: SERVICE_NAME
        value: miniflux
      - name: POSTGRES_USERNAME
        valueFrom:
          secretKeyRef:
            name: miniflux-postgres
            key: username
      - name: POSTGRES_PASSWORD
        valueFrom:
          secretKeyRef:
            name: miniflux-postgres
            key: password
      - name: POSTGRES_HOSTNAME
        value: miniflux-postgres-rw.services.svc.cluster.local
    envFrom:
      - secretRef:
          name: backup
