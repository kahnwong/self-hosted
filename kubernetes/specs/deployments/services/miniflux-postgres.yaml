# maintenance: run `TRUNCATE TABLE sessions;`
name: miniflux-postgres
cnpg:
  image: ghcr.io/cloudnative-pg/postgresql:18.4-standard-trixie
  instances: 1
  database: miniflux
  owner: miniflux
  secret: miniflux-postgres
