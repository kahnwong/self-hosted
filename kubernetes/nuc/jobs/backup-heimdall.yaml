---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup-heimdall
spec:
  schedule: "1 0 * * *"
  jobTemplate:
    metadata:
      name: backup-heimdall
    spec:
      template:
        spec:
          containers:
            - name: backup-heimdall
              image: ghcr.io/kahnwong/docker-aws-backup:ace0f3a
              command: ["sh", "-c"]
              args:
                - |
                  SERVICE_NAME="heimdall"
                  current_date=$(date +%Y-%m-%d)
                  backup_prefix="s3://backup/$current_date"
                  filename="$SERVICE_NAME-$current_date.tar.gz"
                  tar -czf "$filename" /opt/heimdall
                  aws s3 cp --endpoint-url "$S3_ENDPOINT" "$filename" "$backup_prefix/$filename"
                  curl -d "Successfully backup heimdall" https://ntfy.karnwong.me/nuc-backup
              envFrom:
                - secretRef:
                    name: r2
              volumeMounts:
                - name: heimdall
                  mountPath: /opt/heimdall
          restartPolicy: Never
          volumes:
            - name: heimdall
              hostPath:
                path: /opt/heimdall
