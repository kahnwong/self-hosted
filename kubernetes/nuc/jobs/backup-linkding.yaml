---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup-linkding
spec:
  schedule: "1 0 * * *"
  jobTemplate:
    metadata:
      name: backup-linkding
    spec:
      template:
        spec:
          containers:
            - name: backup-linkding
              image: ghcr.io/kahnwong/docker-aws-backup:ace0f3a
              command: ["sh", "-c"]
              args:
                - |
                  SERVICE_NAME="linkding"
                  current_date=$(date +%Y-%m-%d)
                  backup_prefix="s3://backup/$current_date"
                  filename="$SERVICE_NAME-$current_date.tar.gz"
                  tar -czf "$filename" /opt/linkding
                  aws s3 cp --endpoint-url "$S3_ENDPOINT" "$filename" "$backup_prefix/$filename"
                  curl -d "Successfully backup linkding" https://ntfy.karnwong.me/nuc-backup
              envFrom:
                - secretRef:
                    name: r2
              volumeMounts:
                - name: linkding
                  mountPath: /opt/linkding
          restartPolicy: Never
          volumes:
            - name: linkding
              hostPath:
                path: /opt/linkding
