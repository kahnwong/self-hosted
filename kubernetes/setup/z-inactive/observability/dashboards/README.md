# Grafana Dashboards

Get dashboards via <https://play.grafana.org>.

You'll need to update the data source.

JSON dashboards are provided as `original` and `modified` for use with datasource available in `kube-prometheus-stack`.
