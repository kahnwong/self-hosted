---
apiVersion: v1
kind: Service
metadata:
  name: supersecretmessage
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: supersecretmessage
  ports:
    - name: http
      port: 8082
      targetPort: 8082
      nodePort: 30000
