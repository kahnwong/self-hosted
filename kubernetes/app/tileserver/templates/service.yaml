---
apiVersion: v1
kind: Service
metadata:
  name: tileserver
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: tileserver
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30011
