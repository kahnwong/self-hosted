---
apiVersion: v1
kind: Service
metadata:
  name: photoprism
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: photoprism
  ports:
    - name: http
      port: 2342
      targetPort: 2342
      nodePort: 30008
