---
apiVersion: v1
kind: Service
metadata:
  name: ttrss
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: ttrss
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30011
