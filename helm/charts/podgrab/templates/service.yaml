---
apiVersion: v1
kind: Service
metadata:
  name: podgrab
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: podgrab
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30004
