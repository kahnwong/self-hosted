---
apiVersion: v1
kind: Service
metadata:
  name: miniflux
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: miniflux
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30007
