---
apiVersion: v1
kind: Service
metadata:
  name: fava
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: fava
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      nodePort: 30001
