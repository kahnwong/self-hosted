---
apiVersion: v1
kind: Service
metadata:
  name: komga
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: komga
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30002
