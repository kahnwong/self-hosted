---
apiVersion: v1
kind: Service
metadata:
  name: wallabag
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: wallabag
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30009
